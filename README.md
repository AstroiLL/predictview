# BTCAI PredictView

Система визуализации цены Bitcoin с ML-предсказанием направления движения в реальном времени.

## Возможности

- **График цены** — отображение цены BTC/USD (lightweight-charts от TradingView)
- **Объемы торгов** — гистограмма с цветовой индикацией (зелёный/розовый)
- **Множественные VWMA индикаторы** — добавляйте несколько VWMA с разными периодами и цветами
- **Вектор направления** — визуализация ML-предсказания (стрелка вверх/вниз)
- **Автообновление** — опциональное обновление данных каждую минуту
- **Сохранение настроек** — индикаторы сохраняются в localStorage

## Технологический стек

| Компонент | Технология |
|-----------|------------|
| Framework | Vite + React 18 + TypeScript |
| Charts | lightweight-charts (TradingView) |
| State | Zustand |
| Styling | Tailwind CSS |
| Database | Supabase |

## Установка

```bash
npm install
```

## Настройка

Создайте файл `.env` с переменными Supabase:

```env
VITE_SUPABASE_URL=your_supabase_url
VITE_SUPABASE_ANON_KEY=your_supabase_anon_key
```

## Структура данных

Таблица `quotations` в Supabase:

```sql
CREATE TABLE quotations (
  time     TIMESTAMP    PRIMARY KEY,
  close    FLOAT8       NOT NULL,
  vol      FLOAT8       NOT NULL DEFAULT 0,
  dir      INTEGER      NOT NULL DEFAULT 0,
  liq      INTEGER
);
```

| Поле | Описание |
|------|----------|
| `time` | Время свечи |
| `close` | Цена закрытия |
| `vol` | Объем торгов |
| `dir` | Направление (0=вниз, 1=вверх) |
| `liq` | Ликвидность (опционально) |

## Работа с программой

### Запуск

```bash
npm run dev
```

Приложение будет доступно по адресу `http://localhost:5173`

### Интерфейс

**Основная область:**
- График цены BTC/USD с линиями VWMA индикаторов
- Гистограмма объёмов торгов внизу графика
- Прокрутка и масштабирование мышью

**Боковая панель (справа):**

1. **Текущая цена** — отображает последнюю доступную цену BTC

2. **Вектор движения** — стрелка показывающая ML-предсказание:
   - 🟢 Зелёная стрелка вверх — ожидается рост
   - 🔴 Красная стрелка вниз — ожидается падение

3. **VWMA менеджер** — управление индикаторами:
   - Список добавленных индикаторов с периодом и цветом
   - Кнопка "Добавить VWMA" — создаёт новый индикатор со случайным цветом
   - Период по умолчанию: 20
   - Переключатель видимости (скрытые индикаторы сохраняются)
   - Удаление индикатора

4. **Автообновление** — переключатель автоматического обновления данных каждую минуту

### Управление графиком

**Кнопка "To End" (в header):**
- Возвращает график к последним данным
- Полезно после прокрутки истории или при обновлении страницы

**Взаимодействие с графиком:**
- Прокрутка: перетаскивание мышью или колёсико
- Масштабирование: pinch (touch) или Ctrl+колёсико
- Возврат к последним данным: двойной клик на временной шкале

### Горячие клавиши

| Действие | Комбинация |
|----------|------------|
| Масштабировать | Ctrl + колёсико мыши |
| Переместить | Перетаскивание мышью |
| Сброс масштаба | Двойной клик на оси времени |

## VWMA Формула

```
VWMA = Σ(Price × Volume) / Σ(Volume)
```

Период усреднения настраивается при добавлении индикатора. Рекомендуемые значения:
- Короткий: 10-20 (для краткосрочной торговли)
- Средний: 20-50 (для свинг-торговли)
- Длинный: 50-200 (для анализа тренда)

## Сборка

```bash
npm run build
npm run preview
```

## Переменные окружения

| Переменная | Обязательная | Описание |
|------------|--------------|----------|
| `VITE_SUPABASE_URL` | Да | URL вашего Supabase проекта |
| `VITE_SUPABASE_ANON_KEY` | Да | Публичный ключ Supabase |

## Лицензия

MIT
